{
  "framework": null,
  "installCommand": "pnpm install",
  "buildCommand": "BASE_PATH=/ NODE_ENV=production pnpm --filter @workspace/facebook-dashboard run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
