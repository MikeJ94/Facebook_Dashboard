{
  "version": 2,
  "framework": "vite",
  "rootDirectory": "artifacts/facebook-dashboard",
  "installCommand": "pnpm install",
  "buildCommand": "BASE_PATH=/ NODE_ENV=production pnpm run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
