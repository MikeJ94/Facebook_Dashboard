kind = "web"
previewPath = "/"
title = "Facebook Dashboard - Organizaciones Honduras"
version = "1.0.0"
id = "artifacts/facebook-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23883

[services.development]
run = "pnpm --filter @workspace/facebook-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/facebook-dashboard", "run", "build" ]
publicDir = "artifacts/facebook-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23883"
BASE_PATH = "/"
